services:
  dev:
    container_name: dev-intellij
    volumes:
      - ${PWD}/ide/idea:/opt/idea
      - ${PWD}/devcontainer/home/:/home
    shm_size: 4gb
    # Optional resource limits (uncomment to enable)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2.0'
    #       memory: 6G
